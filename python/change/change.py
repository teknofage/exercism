
coins = [1, 4, 5]
target = 12
def find_fewest_coins(coins, target):
    pass


def combinations(coins, k):
    all_combs = []
    # for coin in coins: 
    #     n choose k
    
    
    
    # print (list(combinations_with_replacement (coins, 3)))
    # for combination in list(combinations_with_replacement(coins, 3)):
    #     if sum(combination) <= target: 
    #         all_combs.append(combination)
    #         print(list(all_combs))
        
combinations(coins, 3)
